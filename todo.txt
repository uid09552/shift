1) the validate plan button in optimize view, should also have a "fix button" and a user prompt with additional information which user can provide. Adjust the agent so that the agent can all mcp server can start linear programming optimizer. so create a tool in optimizer (mcp interface) to call and optimize, and also agent should try to move the shift assignment by itself to fix issues and also use users input to optimize.
2) validate the provided ui, remove dublicate views
3) propose additional ui eleements and views for ui which are useful and typically availabe in shift planners.
