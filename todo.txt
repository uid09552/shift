1) adjust planner algorithm, it should plan and consider every user. It should consider the value free day after the shift.
2) if user is not planned, it should place "free" as planned shift in the planner algorithm. for this adjust the rest api in backend service for schedule optimizer and assgned shifts.
3) beautifie the ui, style it improve ux.