1) implement a feature which allows the role shift-admin role to set a date-range, in which users are allowed to place whiches.
a) adjust api spec
b) create backend (service, router, infrastructure, db)
c) implement ui
d) there should be a 3 state  to activate deactive this feature (enable, disable, apply-daterange)
e) only role shift-admin should be able to do that.
f) adjust the documentation