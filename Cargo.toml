[package]
name = "shift"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
# Add other dependencies as needed
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.4", features = ["derive"] }
env_logger = "0.10"
thiserror = "1.0"
axum = { version = "0.7", features = ["multipart"] }
tower = "0.4"
tower-http = { version = "0.5", features = ["cors"] }
figment = { version = "0.10", features = ["env", "yaml"] }
url = "2"

# Database
diesel = { version = "2.1", features = ["postgres", "r2d2", "chrono", "uuid", "serde_json"] }
diesel_migrations = "2.1"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.0", features = ["v4", "serde"] }
async-trait = "0.1"
r2d2 = "0.8"

# Message broker
async-nats = "0.38"

# HTTP client
reqwest = { version = "0.12", features = ["json"] }
base64 = "0.22"
futures-util = "0.3.32"
rust_xlsxwriter = "0.96.0"
calamine = "0.36.0"
# Observability (OpenTelemetry traces + metrics over OTLP, off unless configured)
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
tracing-opentelemetry = "0.33"
opentelemetry = { version = "0.32", features = ["metrics", "trace"] }
opentelemetry_sdk = { version = "0.32", features = ["trace", "metrics"] }
opentelemetry-otlp = { version = "0.32", default-features = false, features = ["grpc-tonic", "tls", "tls-roots", "http-proto", "reqwest-blocking-client", "trace", "metrics", "internal-logs"] }
opentelemetry-semantic-conventions = "0.32"
opentelemetry-http = "0.32"


[dev-dependencies]
tower-test = "0.4"
